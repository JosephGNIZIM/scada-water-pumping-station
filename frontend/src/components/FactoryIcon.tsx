import React from 'react';
import PumpStationLogo from './PumpStationLogo';

const FactoryIcon: React.FC<{ className?: string }> = ({ className }) => <PumpStationLogo className={className} />;

export default FactoryIcon;
